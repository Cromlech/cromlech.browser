cromlech.browser
****************

This package provides definitions (interfaces) for the base browser
components of the Cromlech Framework: `IRenderer` and `IHTTPRenderer`.
It also proposed a specialisation if these interfaces into more concrete
components: `IView` and `ILayout`.

``cromlech.browser`` defines 2 martian directives that can be used to
configure components: `view` and `slot`.
