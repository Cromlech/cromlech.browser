cromlech.browser
****************

``cromlech.browser`` provides definitions (interfaces) for the
components of the Cromlech Toolkit. It also defines 3 martian
directives that can be used to configure components: `view`,
`slot` and `request`.
